# split-domains

Domain lists for EN1GMA split mode.

## Structure

- `prod/` — active domain lists used in production
- `candidates/` — candidate domains for later review/promotion

## Format

One domain suffix per line.

Example:

```
whoer.net
```
